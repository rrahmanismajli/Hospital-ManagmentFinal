@model Hospital_Managment.ViewModels.PrescriptionModelViewBase

@{
    ViewData["Title"] = "Create";
    var Doctors = (List<SelectListItem>)ViewBag.doctors;
    var Patients = (List<SelectListItem>)ViewBag.patients;
    var Appoitments = (List<SelectListitem>)ViewBag.appoitments;
}

<h1>Create</h1>

<h4>Prescription</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label class="control-label">Cakto Pacientin</label>
                <select asp-for="PatientId"  asp-items="@Patients"></select>
            </div>
            <div class="form-group">
                <label  class="control-label">Cakto Doktorin</label>
                <select asp-for="DoctorId"  asp-items="@Doctors"></select>
            </div>
            <div class="form-group">
                <label asp-for="DateTime" class="control-label"></label>
                <input asp-for="DateTime" class ="form-control">
                < asp-validation-for="DateTime" class="text-danger"><</span>
            </div>
            <div class="form-group">
                <label  class="control-label">Cakto Terminin</label>
                <select asp-for="AppoitmentId" asp-items="@Appoitments" />
              
            </div>
            <div class="form-group">
                <label asp-for="Notes" class="control-label"></label>
                <input asp-for="Notes" class="form-control" />
                <span asp-validation-for="Notes" class="text-danger"></span>
            </div>
      
           
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
